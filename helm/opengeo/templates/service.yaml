apiVersion: v1
kind: Service
metadata:
  name: {{ include "opengeo.fullname" . }}
  labels:
    {{- include "opengeo.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opengeo.selectorLabels" . | nindent 4 }}
